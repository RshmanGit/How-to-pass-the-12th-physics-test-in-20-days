---
name: example-skill
description: Template skill — replace with a real one. The description must say WHEN to use the skill so Claude can match it, e.g. "Use when generating a new database migration for the Foo schema."
---

# Example Skill

Skills package reusable, on-demand expertise for a specific kind of task. This body loads only when the
`description` above matches what you're doing.

## When to use
Spell out the triggering conditions precisely. State what kinds of requests should activate this skill.

## Steps
1. ...
2. ...
3. ...

## Notes / gotchas
- Keep each skill focused on one job.
- Reference supporting files with relative paths; they load on demand, not up front.
- Delete this example once you have a real skill.
